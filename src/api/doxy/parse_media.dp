/** @page parse_media Parsing media properties

libquvi uses a @ref m_script to parse the media properties for a
@ref m_url.

@code
quvi_media_t m = quvi_media_new(q, URL);
abort_if_error();
{
  char *s = NULL;
  quvi_media_get(m, QUVI_MEDIA_PROPERTY_TITLE, &s);
  puts(s);
}
quvi_media_free(m); /* Release when done using it. */
m = NULL;
@endcode

@sa @ref query_formats
@sa QuviMediaProperty

The available @ref m_script collection determines which
websites are supported by the library.
*/
